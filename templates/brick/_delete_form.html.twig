<form method="post" action="{{ path('brick_delete', {'id': brick.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ brick.id) }}">
    <button class="btn">Delete</button>
</form>
